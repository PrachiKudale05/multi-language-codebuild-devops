name: Python Demo

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Repository Structure
        run: ls -R

      - name: Run Python
        run: python python/autodeploy.py
